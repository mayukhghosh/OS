THe folder contains a java file, namely link_implement.java. It must be first compiled with the following command:

javac link_implement.java

The following command will run the program after compilation:
java link_implement < [File name]

For example:

java link_implement < input-6

It uses input redirection.